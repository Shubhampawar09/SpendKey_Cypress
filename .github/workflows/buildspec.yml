name: Second Spenkey

on: 
  repository_dispatch:
   types: [start-sub-workflow]
  
  #workflow_dispatch:
  #workflow_run:
  #   workflows: ["dev"]
  #   types: ["completed"]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: print message
        run: |
         echo: "this is my second ymal"
  
